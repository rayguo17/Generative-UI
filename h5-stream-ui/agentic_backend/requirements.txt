# Core
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
openai>=1.40.0
aiohttp>=3.9.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Optional: more accurate token counting (falls back to heuristic if not installed)
tiktoken>=0.5.0

# Card surface screenshots (one-time: python -m playwright install chromium)
playwright>=1.49.0

# Optional: langchain integration (for deep agent extensions)
langchain
langchain-openai
deepagents
deepagents[quickjs]