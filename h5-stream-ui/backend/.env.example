OPENAI_API_KEY=""
OPENAI_MODEL=moonshotai/kimi-k2.5

HOST=127.0.0.1
PORT=8765

# Optional: turn off timing logs with 0/false/no/off
# STREAM_TIMING_LOG=1
# Optional: timeout guards for slow or stalled upstream streams
# FIRST_TOKEN_TIMEOUT_SEC=45
# TOTAL_STREAM_TIMEOUT_SEC=180

# Optional: comma-separated origins, default '*'
# CORS_ORIGINS=*
